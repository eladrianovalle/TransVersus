using System.Collections; using System.Collections.Generic; using UnityEngine; using UnityEngine.UI;   public class GameOverPopUp : MonoBehaviour {  //	public AudioSource audioSource; 	 	[SerializeField] private GameObject GameOverPanel; 	private int timeLeft = 123; 	// Use this for initialization 	void Start () { 		StartCoroutine("PopUpTime"); //		audioSource = GetComponent<AudioSource>(); 	}  	// Update is called once per frame 	void Update () { 		if (timeLeft <= 0) 		{ 		StopCoroutine ("PopUpTime"); 		// Check if interstitial ad is ready 		Debug.Log ("Hello"); 		GameOverPanel.gameObject.SetActive (true); 		// Show it if it's ready  			} 		} 		IEnumerator	PopUpTime() { 		{ 		while (true) 		{ 			yield return new WaitForSecondsRealtime(1); 			timeLeft--; 			} 		} 		} 				// Subscribe to the event 		 		  	 } 